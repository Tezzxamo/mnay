plugins {
    id 'java'
    id 'org.springframework.boot' version '3.3.0'
    id 'io.spring.dependency-management' version '1.1.5'
}


dependencies {
    // 引用api层
    implementation project(':mnay-api')
}

tasks.named('test') {
    useJUnitPlatform()
}
